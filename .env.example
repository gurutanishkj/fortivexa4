# ==============================================================================
# FORTIVEXA TRL 5 - CYBERSECURITY PREDICTIVE-INTELLIGENCE PROTOTYPE
# Environment Configuration Template
# Problem Statement: SIH26184 | Ministry of Home Affairs (MHA) / I4C
# ==============================================================================

# --- Application Server ---
PORT=5001
HOST=0.0.0.0
ENVIRONMENT=development
LOG_LEVEL=INFO

# --- Primary Relational Database (TRL 5 Target: PostgreSQL) ---
# If left commented or unavailable, system automatically degrades to SQLite fallback:
# sqlite:///backend/data/fortivexa_dev.db with live telemetry flag SQLITE_DEV_FALLBACK.
DATABASE_URL=postgresql://fortivexa_user:fortivexa_secure_pass@localhost:5432/fortivexa_db

# --- Graph Database Engine (TRL 5 Target: Neo4j) ---
# If left commented or unavailable, system automatically falls back to in-memory
# NetworkX MultiDiGraph engine with live telemetry flag NETWORKX_ACTIVE.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=fortivexa_neo4j_pass

# --- Cryptographic Security Layer (AES-256-GCM & JWT RBAC) ---
# Generate with: python -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
ENCRYPTION_KEY=dGhpcy1pcy1hLXNlY3VyZS0yNTYtYml0LWtleS1mb3J0aXZleGE=
JWT_SECRET=super-secret-jwt-signing-key-for-fortivexa-trl5-auth-2026
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=480

# --- Frontend Client Configuration ---
VITE_API_BASE=/api
VITE_PORT=5173
